import React from 'react';
import { mount, shallow} from 'enzyme';
import Body from '../client/src/components/Body.jsx';

// describe('property listing body', () => {
//     //shallow rendering
//     it('onClick event got invoked', () => {
//         const wrapper = shallow(<Body />)
//         expect(wrapper.containsMatchingElement(<h4 onClick={handleClick} />)).toBe(true);
//     })
// })